import 'package:polymer/polymer.dart';
import 'dart:html';
export 'package:polymer/init.dart';
@whenPolymerReady
void onReady()
{
  /*var panel = querySelector('#drawerPanel');
  panel.forceNarrow=true;

  var navmenu = querySelector("#naviconmenu");
  var navmenusub = navmenu.onClick.listen(
          (event) =>panel.togglePanel());

  var navmain = querySelector("#navicon");
  var navmainsub = navmain.onClick.listen(
          (event) =>panel.togglePanel());*/
}

/*
main() => initPolymer().then((zone) => zone.run(setListeners));
setListeners()
{
  var elem = querySelector("#searchbutton");
  var subscription = elem.onClick.listen(
          (event) => window.alert('click!'));

  var panel = querySelector('#drawerPanel');
  panel.attributes['forceNarrow']="true";

  var navmenu = querySelector("#naviconmenu");
  var navmenusub = navmenu.onClick.listen(
          (event) =>(panel as CoreDrawerPanel).togglePanel());

  //querySelector('#drawerPanel').forceNarrow=true;


  //panel.forceNarrow = true;
  //drawerPanel.forceNarrow=true;
}
*/